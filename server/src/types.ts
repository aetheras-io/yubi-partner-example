export type JankenMove = 'rock' | 'paper' | 'scissors';
export type JankenResult = 'win' | 'lose' | 'draw';
export interface UserState {
  id: string;
  yubiLink: string | null;
}
export interface UserState {
  id: string;
  username: string;
  balance: number;
}
